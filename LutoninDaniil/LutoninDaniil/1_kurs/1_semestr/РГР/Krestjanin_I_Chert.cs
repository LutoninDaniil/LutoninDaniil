using System;
/*
class Krestjanin_I_Chert
{
    static void Main(string[] japi)
    {
        Console.Write("Ввод: ");
        int n = Convert.ToInt32(Console.ReadLine());
        int c = 0 ;

        for(int i = 1; i<=n; i++)
        {
            for(int j = i+1; j<=i*2; j++)
            {
                for(int k = 1; i*termopasta(2,k)-j*(termopasta(2,k)-1)>=0; k++)
                {
                    if(i*termopasta(2,k)-j*(termopasta(2,k)-1) == 0)
                    {
                        c+=1;
                        break;
                    }
                }
            }
        }

        Console.WriteLine("Ответ: " + c);
    }
    static int termopasta(int o, int q)
    {
        int r = 1;
        for(int i = 0; i<q; i++)
        {
            r*=o;
        }
        return r;
    }
}
*/